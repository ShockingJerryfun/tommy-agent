# MEMORY

当前公开版本不内置用户个人记忆。

## 记忆边界

- 运行时产生的会话、工具调用、审批、长期记忆和检查点保存在本地 `data/` 相关目录中。
- 只有经过确认且适合公开的模板内容可以提交到仓库。
- 如果用户要求保存长期记忆，应通过应用内的记忆提案流程处理，而不是直接伪造“已记住”状态。

## 默认上下文

- 项目包含 FastAPI + LangGraph 后端。
- 项目包含 Next.js + React 前端。
- 前后端通过本地 API 代理协同运行。
